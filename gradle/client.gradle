task 'npm install'(type: Exec) {
  workingDir './client'
  executable 'bash'
  args '-c', 'npm install'
}

task 'npm run build'(type: Exec, dependsOn: ['npm install']) {
  workingDir './client'
  executable 'bash'
  args '-c', 'npm run build'
}
